{% extends 'base.html.twig' %}

{% block title %}TypeDeReseau{% endblock %}

{% block body %}
    <h1>TypeDeReseau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_de_reseau.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_de_reseau.nom }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ type_de_reseau.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_de_reseau_index') }}">back to list</a>

    <a href="{{ path('app_type_de_reseau_edit', {'id': type_de_reseau.id}) }}">edit</a>

    {{ include('type_de_reseau/_delete_form.html.twig') }}
{% endblock %}
