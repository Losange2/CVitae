<form method="post" action="{{ path('app_reseau_delete', {'id': reseau.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reseau.id) }}">
    <button class="btn">Delete</button>
</form>
