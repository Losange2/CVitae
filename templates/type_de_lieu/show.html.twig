{% extends 'base.html.twig' %}

{% block title %}TypeDeLieu{% endblock %}

{% block body %}
    <h1>TypeDeLieu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_de_lieu.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_de_lieu.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_de_lieu_index') }}">back to list</a>

    <a href="{{ path('app_type_de_lieu_edit', {'id': type_de_lieu.id}) }}">edit</a>

    {{ include('type_de_lieu/_delete_form.html.twig') }}
{% endblock %}
